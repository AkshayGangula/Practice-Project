name: Workflow Run Example
on: 
    workflow_run:
        workflows: [Deployment Exercise 1]
        types: [completed]
jobs:
    echo-string:
        runs-on: ubuntu_latest
        steps:
            - run: echo "I was trigged because 'Deployment Exercise' 1 was completed"

